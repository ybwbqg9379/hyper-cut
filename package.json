{
  "name": "hypercut",
  "packageManager": "bun@1.2.18",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "turbo run dev --filter=@hypercut/web",
    "build:web": "turbo run build --filter=@hypercut/web",
    "dev:tools": "turbo run dev --filter=@hypercut/tools",
    "build:tools": "turbo run build --filter=@hypercut/tools",
    "start:tools": "turbo run start --filter=@hypercut/tools",
    "lint:web": "bunx --bun @biomejs/biome lint apps/web/src --max-diagnostics=1000",
    "lint:web:fix": "bunx --bun @biomejs/biome lint apps/web/src --write --max-diagnostics=1000",
    "format:web": "bunx --bun @biomejs/biome format apps/web/src/services/renderer --write --max-diagnostics=1000",
    "test": "bun test"
  },
  "dependencies": {
    "@types/react": "^19.2.10",
    "@types/react-dom": "^19.2.3",
    "better-auth": "^1.4.15",
    "next": "^16.1.3"
  },
  "devDependencies": {
    "turbo": "^2.7.5",
    "typescript": "5.8.3"
  },
  "trustedDependencies": [
    "@tailwindcss/oxide"
  ]
}
